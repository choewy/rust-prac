# Install(MacOS)

```
$ curl https://sh.rustup.rs -sSf | sh
$ source $HOME/.cargo/env
$ export PATH="$HOME/.cargo/bin:$PATH"
$ rustc --version
```

# Compile

```
$ rustc ${FILENAME}.rs
```

# Run

```
$ ./${FILENAME}
```
